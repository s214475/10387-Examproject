#### 

# 1 cd ~/10387
# 2 source .venv/bin/activate
# 3 export PATH="$PWD/.uv-tool/bin:$PATH"
# 4 git init
# 5 
git config --global user.name "s214475"
git config --global user.email "s21445@dtu.dk"
# 6 git status
# 7 git log
# git commit -m "Add feature this and that"
# git checkout -m testbranch (Create branch)
# git checkout -d testbranck (Delete branch)
# git checkout "branch name"
# echo ".uv-tool/" >> .gitignore
# git add .gitignore
# git commit -m "Add project files"
